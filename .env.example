# Database
DATABASE_URL=postgresql://homeFin:password@postgres:5432/homeFin
POSTGRES_USER=homeFin
POSTGRES_PASSWORD=change_me

# Redis
REDIS_URL=redis://redis:6379/0

# Ollama — external, not managed by this compose file
OLLAMA_BASE_URL=http://host.docker.internal:11434
OLLAMA_MODEL=llama3

# App
SECRET_KEY=change_me_to_a_random_string
FIELD_ENCRYPTION_KEY=change_me_32_bytes_exactly_______

# Dev only — exposes rendered prompts at /admin/prompts
EXPOSE_PROMPTS=false